#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
// #define end \n
#define IOS                  \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(0);
#define ll long long
#define ld long double
// ============================================================================================================================================================================
bool isPerfectSq(ll n)
{
    ll s = sqrt(n);

    return (s * s == n);
}
// ============================================================================================================================================================================
#define max3(a, b, c) max(a, max(b, c))
#define max4(a, b, c, d) max(a, max(b, max(c, d)))
#define min4(a, b, c, d) min(a, min(b, min(c, d)))
#define min3(a, b, c) min(a, min(b, c))
#define min5(a, b, c, d, e) min(a, min(b, min(c, min(d, e))))
// ============================================================================================================================================================================
const int N = 1e5 + 1;
// ============================================================================================================================================================================
ll in()
{
    ll x;
    cin >> x;
    return x;
}
int In()
{
    int x;
    cin >> x;
    return x;
}
// ============================================================================================================================================================================

// ============================================================================================================================================================================
bool isPrime(ll n)
{
    if (n <= 1)
        return false;
    if (n <= 3)
        return true;
    if (n % 2 == 0 || n % 3 == 0)
        return false;
    for (int i = 5; i * i <= n; i = i + 6)
        if (n % i == 0 || n % (i + 2) == 0)
            return false;
    return true;
}
// ============================================================================================================================================================================
void init(ll arr[], ll n)
{
    for (ll i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
}
void Init(int arr[], int n)
{
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
}
// ============================================================================================================================================================================
bool sameP(ll a,ll b)
{
    return (a%2==b%2);
}
// ============================================================================================================================================================================

void solve()
{
   ll n,ce=0,co=0,count=0;
   cin>>n;
   ll arr[n];
   for(ll i=0;i<n;i++)
   {
       cin>>arr[i];
       if(arr[i]%2==0)
       {
           ce++;
       }
       else{
           co++;
       }
   }
   if(n==1)
   {
       cout<<0<<endl;
       return;
   }
   if(n%2==1)
   {
       if(abs(ce-co)!=1)
       {
           cout<<-1<<endl;
           return;
       }
       else{
           bool check = false;
           for(ll i=0;i<n;i++)
           {
               for(ll j=1;j<n-1;j++)
               {
                   if(sameP(arr[j],arr[j-1])&&!sameP(arr[j],arr[j+1])){
                       swap(arr[j],arr[j+1]);
                       count++;
                       j++;
                       check=true;
                   }
               }
               if(check==false){
                    if(n>=3&&sameP(arr[n-1],arr[n-2])&&!sameP(arr[n-2],arr[n-3])){
                        count++;
                    }
                   cout<<count<<endl;
                   return;
               }
               check=false;
           }
                    if(n>=3&&sameP(arr[n-1],arr[n-2])&&!sameP(arr[n-2],arr[n-3])){
                        count++;
                    }
           cout<<count<<endl;
       }

   }
   else{
       if(ce!=co)
       {
           cout<<-1<<endl;
           return;
       }
       else{
           bool check = false;
           for(ll i=0;i<n;i++)
           {
               for(ll j=1;j<n-1;j++)
               {
                   if(sameP(arr[j],arr[j-1])&&!sameP(arr[j],arr[j+1])){
                       swap(arr[j],arr[j+1]);
                       count++;
                       j++;
                       check=true;
                   }
               }
               if(check==false){
                    if(n>=3&&sameP(arr[n-1],arr[n-2])&&!sameP(arr[n-2],arr[n-3])){
                        count++;
                    }
                   
                   cout<<count<<endl;
                   return;
               }
               
               check==false;
           }

            if(n>=3&&sameP(arr[n-1],arr[n-2])&&!sameP(arr[n-2],arr[n-3])){
                count++;
            }
           cout<<count<<endl;
       }
   }

}

// ============================================================================================================================================================================

int32_t main()
{
    IOS;
    ll t;
    cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}

// ============================================================================================================================================================================